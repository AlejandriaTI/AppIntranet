export interface Asesoria {
  id: string;
  profesion: string;
}

export interface Reunion {
  id: string;
  titulo: string;
  fecha_reunion: string;
  meetingId: string;
  enlace_zoom: string;
}

export interface Induccion {
  id: string;
  titulo: string;
  url: string;
}
